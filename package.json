{
  "name": "calibpro",
  "version": "1.0.0",
  "description": "CalibPro — Calibration Management System",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
